pipeline{
    agent any
    stages{
        stage("git"){
            agent{
                label 'slave'
            }
            steps{
                git 'https://github.com/manepallivinay/JavaWebCalculator.git'
                
            }
        }
    
        stage("maven build"){
            steps{
                sh "cd /home/ec2-user/workspace/build"
                sh "mvn clean package"
                
            }
        }
    }
}
